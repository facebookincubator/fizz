load("@shim//build_defs:prebuilt_cpp_library.bzl", "prebuilt_cpp_library")

LIBS = [
    "advapi32.lib"
]
[
    (
        prebuilt_cpp_library(
            name = lib,
            visibility = ["PUBLIC"],
        )
    )
    for lib in LIBS
]

